<%= form_for(@rua) do |f| %>
  <% if @rua.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rua.errors.count, "error") %> prohibited this rua from being saved:</h2>

      <ul>
      <% @rua.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome_rua %><br>
    <%= f.text_field :nome_rua %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
